{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. It needs nothing beyond the app's own engine command, so no plugin or core permission is granted.",
  "windows": ["main"],
  "permissions": []
}
