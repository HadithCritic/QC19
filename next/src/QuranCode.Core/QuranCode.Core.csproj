<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.12" />
  </ItemGroup>

  <ItemGroup>
    <!-- The findings travel with the engine, so a sidecar needs no data file
         beside it. Adding a finding is editing this file. -->
    <EmbeddedResource Include="Code19/findings.tsv" />
  </ItemGroup>

</Project>
