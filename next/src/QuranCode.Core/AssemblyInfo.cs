using System.Runtime.CompilerServices;

// The digit-transform helpers are implementation detail, but they are also
// exactly the kind of arithmetic that needs direct unit coverage rather than
// being exercised only through a full-book valuation.
[assembly: InternalsVisibleTo("QuranCode.Core.Tests")]
